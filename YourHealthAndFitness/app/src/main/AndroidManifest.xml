<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.astrosei.yourhealthandfitness">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".DietJournalActivity" />
        <activity android:name=".LogJournalActivity" />
        <activity android:name=".ExercisesActivity" />
        <activity android:name=".ProgramActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".VisualizeActivity" />
        <activity android:name=".ExercisesTutorialActivity" />
        <activity android:name=".UpdateExerciseOfTodayActivity" />
        <activity android:name=".UpdateProfileActivity"></activity>
    </application>

</manifest>